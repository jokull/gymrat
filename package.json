{
  "name": "gymrat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "prebuild": "turbo prune --scope=next && cd ./out && pnpm --filter api remove wrangler @miniflare/tre @cloudflare/d1 && pnpm install -r && vercel build --yes --prod --cwd apps/next",
    "vercel": "cd out/apps/next && vercel deploy --prebuilt --prod"
  },
  "keywords": [],
  "author": "Jökull Sólberg",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
